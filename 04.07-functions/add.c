/* NOTE: This defines a function named "add" that takes two integers and
 *       returns an integer; the curly braces surrounding the body create a new
 *       scope; the top of a function is a valid place to declare locals. */
int add(int x, int y) {
    int z = x + y;

    return z;
}
